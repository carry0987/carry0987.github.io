export interface NipponColor {
    id: number;
    name: string; // Kanji name
    romaji: string; // Romaji name
    hex: string; // Hex code
    rgb: string; // R, G, B
    cmyk: string; // C, M, Y, K
}
